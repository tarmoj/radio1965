// Empty on purpose: its presence makes Xcode link the Swift runtime and
// compatibility shims (e.g. swiftCompatibility56) that Firebase's binaries need,
// even though this app has no Swift code of its own.
